# Copy this file to `.env` and fill in real values. `.env` is git-ignored.
# Claude reads keys from here. Never paste a key into any other file.

# GitHub personal access token (classic). Used to create and push to your private OS repo.
GITHUB_TOKEN=
GITHUB_USER=

# Vercel token (full account scope). Used to deploy anything you build.
VERCEL_TOKEN=
